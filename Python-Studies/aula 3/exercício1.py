#Crie um programa que leia um numero real qualquer pelo teclado e mostre na tela sua porção inteira

from math import trunc

num_real = (float(input("Digite um número: ")))

print (trunc(num_real))